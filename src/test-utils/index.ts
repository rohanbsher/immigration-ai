/**
 * Test utilities barrel export.
 *
 * - factories.ts: Test data generators (MockFile, createMockCase, etc.)
 * - render.tsx: Custom render with providers (if exists)
 */
export * from './factories';
// export * from './render';  // Uncomment when render.tsx is created
